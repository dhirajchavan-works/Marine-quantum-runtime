# marine_quantum_runtime — Quantum Runtime Capability
# BHIV Ecosystem | Marine Intelligence System
#
# Python >= 3.8
#
# CORE RUNTIME (no external dependencies — stdlib only):
#   All 4 original entry points (run_signal, run_quantum_pipeline,
#   run_distributed_qapp, run_operational_drift) run with zero installs.
#   The governance layer (run_governance, 46 checks) also requires no installs.
#
# RECOMMENDED (real Aer simulation — tested and confirmed working):
qiskit>=1.0.0
qiskit-aer>=0.14.0
#
# OPTIONAL (for IBM Runtime execution — requires IBM_QUANTUM_TOKEN):
# qiskit-ibm-runtime>=0.20.0
#
# INSTALL (development / CI):
#   pip install qiskit qiskit-aer --break-system-packages
#
# No other external dependencies. No databases. No message brokers.
# The ecosystem integration test (run_ecosystem_integration.py, 33 checks)
# requires qiskit + qiskit-aer for Phase 7.3 (Aer determinism) and Phase 7.4b
# (identical execution surface proof). Without them it falls through to
# local_simulator for those checks and the Aer-specific assertions fail.
